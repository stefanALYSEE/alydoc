[build]
  publish = "."

# Service Worker - kein Cache
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
    Content-Type = "application/javascript"

# Manifest
[[headers]]
  for = "/manifest.json"
  [headers.values]
    Cache-Control = "no-cache"
    Content-Type = "application/manifest+json"

# Add-In HTML - KEIN X-Frame-Options, CORS für Outlook
[[headers]]
  for = "/addin.html"
  [headers.values]
    Cache-Control = "no-cache"
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"
    Content-Security-Policy = "frame-ancestors https://*.office.com https://*.outlook.com https://*.microsoft.com https://alydoc.netlify.app https://alydoc.alysee.de"

# Auth Dialog für Outlook Add-in
[[headers]]
  for = "/auth.html"
  [headers.values]
    Cache-Control = "no-cache"
    Access-Control-Allow-Origin = "*"
    Content-Security-Policy = "frame-ancestors https://*.office.com https://*.outlook.com https://*.microsoft.com https://alydoc.netlify.app https://alydoc.alysee.de"

# Alle HTML-Dateien – kein Caching
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Pragma = "no-cache"
    X-Content-Type-Options = "nosniff"

# Haupt-App HTML (X-Frame-Options zusaetzlich)
[[headers]]
  for = "/index.html"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"

# Icons
[[headers]]
  for = "/*.png"
  [headers.values]
    Cache-Control = "public, max-age=86400"
    Access-Control-Allow-Origin = "*"
